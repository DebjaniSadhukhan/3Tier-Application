version: 0.2

phases:
  install:
    commands:
      - pip install cfn-lint

  build:
    commands:
      - aws cloudformation validate-template --template-body file://main.yaml
      - cfn-lint main.yaml